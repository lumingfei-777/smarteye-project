**MAV无人机检测系统架构**

---

**输入层**
```
摄像头/视频流
    ↓
OpenCV 视频捕获 (cv2.VideoCapture)
```

---

**检测与追踪层（主线程）**
```
原始帧
  ├─ YOLO26s 目标检测 (ByteTrack多目标追踪)
  │    └─ 输出: bbox, track_id, confidence
  │
  ├─ Metric3D ViT-Small 深度估计 (ROI模式)
  │    └─ 输出: 每个目标的深度值Z
  │
  └─ 3D坐标反投影 (pixel_to_3d)
       └─ 输出: [X, Y, Z] 世界坐标
```

---

**滤波与状态估计层**
```
每个目标 → KalmanFilter3D
  状态向量: [X, Y, Z, Vx, Vy, Vz, Ax, Ay, Az]
  ├─ predict()  每帧预测
  ├─ update()   有检测时更新
  └─ 输出: 平滑位置 / 速度 / 加速度 / 预测轨迹
  
DroneTrack 管理每个目标
  ├─ 遮挡处理 (最多10帧靠预测维持)
  └─ 轨迹历史 (raw / filtered / predicted)
```

---

**自适应微调层（守护线程）**
```
FinetuneDaemon (后台线程)
  │
  ├─ 采集阶段 (实时, 非阻塞)
  │   ├─ 过滤: confidence > 0.85, 非遮挡, 尺寸够大
  │   ├─ SceneFeatureDB 两阶段去重
  │   │   ├─ 第一阶段: 多特征分类场景类型
  │   │   │   (亮度/对比度/纹理/能见度)
  │   │   │   → 正常 / 大雾 / 薄雾 / 夜间 / 低纹理
  │   │   └─ 第二阶段: 同类型桶内pHash去重
  │   ├─ 保存真实样本 (YOLO格式)
  │   └─ AlphaBlendSynthesizer 合成样本
  │       (crop池 → Alpha混合+高斯模糊 → 植入背景)
  │
  ├─ 触发条件 (同时满足)
  │   ├─ 连续5秒无目标 (空闲)
  │   ├─ 距上次微调 > 5分钟
  │   ├─ 训练样本 ≥ 30
  │   └─ 当前无微调在运行
  │
  └─ FinetuneExecutor (独立线程)
      ├─ 备份当前权重
      ├─ 分层学习率训练
      │   ├─ 层0~4   冻结
      │   ├─ 层5~10  lr × 0.1 (BN/bias不decay)
      │   └─ 层11+   正常lr   (BN/bias不decay)
      ├─ 余弦退火调度 (step级别)
      ├─ 验证新权重
      └─ 热加载 → 覆盖原模型
```

---

**输出层**
```
├─ 标注视频帧
│   ├─ bbox + ID + 距离/速度标签
│   ├─ 滤波轨迹 (蓝色)
│   └─ 预测轨迹 (黄色)
│
├─ SocketClient (后台发送线程)
│   ├─ 视频帧 (JPEG压缩 base64, 限速10fps)
│   └─ 检测数据 (JSON: 位置/速度/遮挡状态/预测轨迹)
│
└─ 前端监控界面 (WebSocket)
    ├─ 实时视频显示
    ├─ 目标态势列表
    ├─ 3D轨迹俯视图
    ├─ 距离/速度/目标数图表
    └─ 雷达显示
```

---

**硬件**
```
Jetson Orin NX 
  ├─ GPU: YOLO推理 + Metric3D推理 + 微调训练
  ├─ CPU: OpenCV / Socket / 守护线程
  └─ 存储: 数据集 / 权重备份 / 日志
```
